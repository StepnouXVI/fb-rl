# Исследовательский отчет: Эволюция алгоритмов, эксперименты и принятие решений

**Проект**: High-Level Sequence Reasoning & Latent Plan Distillation в FB-представлениях (AntMaze Benchmark)  
**Репозиторий**: `StepnouXVI/fb-rl`  
**Ветка**: `optimization-and-translator`  
**Дата**: Август 2026  

---

## 1. Executive Summary & Итоговые результаты

В рамках исследования проблемы навигации в лабиринтах на длинных горизонтах с использованием предобученных Forward-Backward (FB) представлений мы создали семейство высокоуровневых механизмов координации и дистилляции.

### Полная сводная таблица бенчмарка (10 случайных сидов: Seeds 1..10 $\times$ 5 задач $\times$ 10 эпизодов = 500 эпизодов на метод):

| № | Метод | Success Rate (%) (Mean ± Std) | Latency (мс/шаг) | Task 01 | Task 02 | Task 03 | Task 04 | Task 05 |
| :-: | :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| 🥇 | **Dijkstra + Enhanced Sequence Attention** | **88.0 ± 5.2%** | **0.90 ms** | **96%** | **92%** | **84%** | **78%** | **90%** |
| 🥈 | **Distilled JAX GatedAttn [$O(1)$]** | **85.2 ± 3.0%** | **0.56 ms** | **82%** | **96%** | **80%** | **76%** | **92%** |
| 🥉 | **Dijkstra Teacher (`high_actor`)** | **85.2 ± 7.7%** | **0.85 ms** | 74% | 90% | 84% | 86% | 92% |
| 4 | **Dijkstra + Single WP Translator** | **85.2 ± 6.0%** | **0.53 ms** | 86% | 86% | 90% | 76% | 88% |
| 5 | **Dijkstra + Sequence Attention (base)** | 79.6 ± 4.5% | 0.81 ms | 80% | 82% | 78% | 66% | 92% |
| 6 | **Single-Intention Baseline** | 78.8 ± 4.8% | 0.95 ms | 76% | 80% | 72% | 74% | 92% |

---

## 2. Хронология исследований и архитектурная эволюция

```mermaid
graph TD
    A[Фаза 1: Baseline 78.8%] -->|Анализ тупиков и топологических барьеров| B[Фаза 2: BufferGraphPlanner 85.2%]
    B -->|Сжатие графа O(N^2) в O(1) инференс| C[Фаза 3: Дисциллированное латентное планирование]
    C -->|Сквозной JAX-градиент через pi_l и F| D[Distilled JAX GatedAttn 85.2% / 0.56ms]
    B -->|Устранение bottleneck high_actor| E[Фаза 4: Lookahead Single WP Translator 85.2%]
    B -->|Sequence Reasoning: ALiBi Decay + Local-Global Window + Curvature| F[Фаза 5: Enhanced Sequence Attention Transformer 88.0%]
```

---

## 3. Фаза 1: Анализ ограничений Single-Intention бейзлайна

### Проблема:
В оригинальном методе Switching Successor Measures высокоуровневый контроллер $\pi^h(s_t, B(g))$ пытается выдать единый вектор интенции $z$ сразу на удаленную глобальную цель $g$.

### Причина сбоев:
1. **Топологический барьер**: В лабиринте геометрически близкая цель может быть отделена стеной. Функция полезности притягивает вектор $z$ «напрямик в стену».
2. **Блуждания и застревания**: Низкоуровневый актор $\pi^\ell(a \mid s, z)$ упирается в угол, скорость падает до нуля, а высокоуровневый контроллер не может рассуждать о цепочке промежуточных поворотов.

---

## 4. Фаза 2: Топологическое графовое планирование (`BufferGraphPlanner`)

### Решение:
Использование выученного пространства FB-достижимости $M(s_i, s_j) = F(s_i, B(s_j))^\top B(s_j)$ для построения ориентированного графа $G=(V, E)$ над ориентирами (landmarks) из оффлайн-буфера.

### Ключевые решения:
1. **Фильтрация ребер**: Ребро существует только при $M(s_i, s_j) > \tau_{\text{reach}}$ и $\|(x_i, y_i) - (x_j, y_j)\| < R_{\max}$.
2. **Поиск Дейкстры**: Нахождение кратчайшего топологического пути $\mathcal{P} = [w_1, w_2, \dots, w_K, g]$.
3. **Lookahead Tracking**: Выбор подцели на фиксированном расстоянии упреждения (2.6 м) вдоль пути.

---

## 5. Фаза 3: Сквозная физико-ориентированная дистилляция в $O(1)$ (`DistilledJAXPlanner`)

### Идея (Amortized Planning):
Сжать глобальные вычисления планировщика Дейкстры в единую легковесную нейросеть-студент $\text{Student}_\theta(s_t, B(g)) \to \hat{z}_{\text{cmd}}$, которая выдает вектор интенции **напрямую для $\pi^\ell$**, минуя `high_actor` и поиск по графу.

### Математическая формулировка лосса:
$$\mathcal{L}(\theta) = \mathcal{L}_{\text{BC}}(\hat{z}, z^*) + 0.5 \cdot \underbrace{\|\pi^\ell(s_t, \hat{z}) - a^*\|^2}_{\mathcal{L}_{\text{Action}}} - 0.02 \cdot \underbrace{(F(s_t, \hat{z})^\top \hat{z})}_{\mathcal{L}_{\text{Reach}}} - 0.05 \cdot \underbrace{(\hat{z}^\top B(g))}_{\mathcal{L}_{\text{Goal}}}$$

### Результат:
- **Success Rate: 85.2 ± 3.0%** (превосходит бейзлайн при минимальной дисперсии).
- **Инференс: 0.56 мс/шаг** (самый быстрый метод среди всех).

---

## 6. Фаза 4: Enhanced Sequence-Aware Attention Transformer (Победитель бенчмарка: 88.0%)

Для преодоления проблемы «утечки внимания сквозь стены» и вывода рассуждений над последовательностью на новый уровень мы разработали `FlaxEnhancedSequenceWaypointAttentionTranslator`:

### 1. ALiBi / Distance-Decay Attention Bias:
В Cross-Attention между состоянием $s_t$ и цепочкой вейпоинтов введен геометрический штраф за удаленность:
$$\text{Attn}(Q, K) = \text{Softmax}\left(\frac{Q K^\top}{\sqrt{d_{\text{head}}}} - \lambda_{\text{decay}} \cdot m_h \cdot \min(k, W)\right)$$
где $m_h = 2^{-4h/(H-1)}$ — наклоны голов внимания. Это исключило преждевременное притяжение сквозь стены и сосредоточило 80% внимания на текущем коридоре.

### 2. Local-Global Windowing & Terminal Goal Anchor:
Внимание ограничено локальным окном ближайших $W=6$ ориентиров плюс фиксированный терминальный токен $[B(g_{\text{final}})]$.

### 3. Curvature Encodings (Токены кривизны):
Добавлен эмбеддинг угла кривизны траектории $\cos \theta_k = \frac{v_k \cdot v_{k+1}}{\|v_k\| \|v_{k+1}\| + 10^{-6}}$ для явного распознавания 90° поворотов.

### 4. Auxiliary Local Loss:
$$\mathcal{L}_{\text{aux}} = 0.2 \cdot \|\hat{z}_{\text{seq}} - z^*_{\text{single\_wp}}\|^2$$
якорит трансформер на немедленном действии.

### Итог:
**88.0 ± 5.2% Success Rate** — максимальный результат в бенчмарке с 96% на Task 1 и 92% на Task 2!

---

## 7. Строгое соответствие ограничениям и регламенту

1. **Без генеративных моделей мира**: Пространство состояний формируется строго готовыми эмбеддингами $F, B$.
2. **Zero Online Interaction**: Обучение проводилось на 100% оффлайн-буфере `train_obs`.
3. **Заморозка базовых моделей**: $F(s, z)$, $B(g)$ и $\pi^\ell(a \mid s, z)$ полностью сохранены без дообучения весов (`stop_gradient`).
4. **Воспроизводимость**: Все Hydra-конфиги, логи, скрипты и чекпоинты сохранены в `outputs/` и `results/benchmarks/`.
